# DedScope, a memory manipulation library (Maintenance Mode)

A lightweight Windows `Memory Scanner` and `Process Manager` written in modern C++20.

Features

- Process `Enumeration`
- Process `Attachment`
- Memory `Reading`
- Memory `Writing`
- Value Scanner `(planned)`